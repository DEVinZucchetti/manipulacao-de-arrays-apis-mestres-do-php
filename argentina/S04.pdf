 
  PS C:\Users\julio> docker 

  run --name api_places -e POSTGRESQL_USERNAME=docker
   -e POSTRESQL_PASSWORD=docker -e POSTGRESQL_DATABASE=api_places_database 
   -p 5432:5432 bitnami/postgresql

  CREATE TABLE "places" (
  id serial PRIMARY KEY,
  name varchar(150) NOT NULL,
  contact varchar(32),
  opening_hours varchar(120),
  description text,
  latitude float UNIQUE NOT NULL,
  longitude float UNIQUE NOT NULL,
  created_at timestamp with time zone DEFAULT now()
);
  
  CREATE TABLE "reviews" (
  id serial PRIMARY KEY,
  place_id integer,
  name text NOT NULL,
  email varchar(150),
  stars decimal(2,1),
  date timestamp,
  status status not null default'PENDENTE',
  created_at timestamp with time zone DEFAULT now(),
  foreign KEY  ("place_id") REFERENCES "places" (id)
);
insert into places 
(name,
 contact ,
 opening_hours,
 description,
 latitude,
 longitude)
values 
('noma sushi',
'48-98998999',
'19:00-02:00',
'o melhor sushi da região',
-27.6037251,
48.6281259);

select * from places

select * from PLACES where id = 1;

DELETE from places where id = 1;


